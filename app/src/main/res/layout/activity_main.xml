<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="fylder.recycler.demo.MainActivity">

    <Button
        android:id="@+id/main_demo_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/accent"
        android:text="demo loading"
        android:textColor="@color/font_white" />

    <Button
        android:id="@+id/main_fish_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/main_demo_btn"
        android:layout_marginTop="8dp"
        android:background="@color/accent"
        android:text="fish loading"
        android:textColor="@color/font_white" />

    <Button
        android:id="@+id/main_grid_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/main_fish_btn"
        android:layout_marginTop="8dp"
        android:background="@color/accent"
        android:text="grid"
        android:textColor="@color/font_white" />

</RelativeLayout>
